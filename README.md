# tsdown-starter

A starter for creating a TypeScript package.

## Development

- Install dependencies:

```bash
yarn install
```

- Run the unit tests:

```bash
yarn vitest
```

- Build the library:

```bash
yarn tsdown
```
